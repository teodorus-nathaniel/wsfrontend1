export const fish = 'this is a fish'
export const goat = 'this is goat'

export let sheep = 'sheep baby'
if (sheep === 'sheep baby') {
  sheep = 'sheep adult'
}

const chicken = 'chicken'
export default chicken

// kalo misal di dalem file ini mau export banyak hal,
// pake named export

// kalo cman 1 boleh default export, boleh named export
